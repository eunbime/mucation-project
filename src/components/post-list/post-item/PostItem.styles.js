import styled from 'styled-components';

const StPostItemWrapper = styled.li`
display: flex;
  width: 100%;
  height: 250px;
  background-color: var(--subColor);
`;

const StPostThumbnail = styled.img`
  width: 20vh;
`;

const StPostText = styled.div`
  margin-top: 25px;
  background-color: ;
  width: 100%;
`;

export { StPostItemWrapper, StPostThumbnail, StPostText };
